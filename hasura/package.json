{
  "name": "hasura",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "hasura": "yarn install --check-files && hasura --skip-update-check"
  },
  "dependencies": {
    "hasura-cli": "^2.36.1"
  }
}
